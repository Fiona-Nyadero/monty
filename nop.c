#include "monty.h"

/**
 * nop - this function doesn't do anything
 *
 * @stack: point top of stack
 * @line_number: instruction appears here
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
